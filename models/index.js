// Import the User and Thought models from their respective modules
const User = require('./User');
const Thought = require('./Thought');

// Export the User and Thought models
module.exports = { User, Thought };
